const Book = require('./book.model');
const Member = require('./member.model');
const Transaction = require('./txn.model');
const Reservation = require('./res.model');
const User = require('./user.model');

module.exports = {
  Book,
  Member,
  Transaction,
  Reservation,
  User,
};
